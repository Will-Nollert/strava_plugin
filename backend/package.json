{
  "name": "strava-plugin-oauth-proxy",
  "version": "1.0.0",
  "description": "Secure OAuth proxy for Strava Plugin Chrome extension",
  "main": "index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "jest",
    "deploy": "sam deploy --guided",
    "clean": "rm -rf node_modules",
    "predeploy": "npm ci --production"
  },
  "dependencies": {
    "axios": "^1.8.3",
    "aws-sdk": "^2.1692.0"
  },
  "devDependencies": {
    "jest": "^27.4.5"
  }
}
